name: dev_blog

services:
  mongodb:
    image: docker.io/mongo:latest
    restart: no
    volumes:
      - mongodb-configdb:/data/configdb
      - mongodb-db:/data/db
    ports:
      - '127.0.0.1:27017:27017'

volumes:
  mongodb-configdb:
  mongodb-db:
